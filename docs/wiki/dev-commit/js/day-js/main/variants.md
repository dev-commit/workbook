# Варианты

```js
const a = dayjs().add(40, "days").format("...");
const a = dayjs().subtract(40, "day").format("...");
```

```js
const a = dayjs().startOf("day");
const a = dayjs().endOf("day");
const a = dayjs().year();
const a = dayjs().month();
```
