# indexOf(), lastIndexOf() - поиск в строке совпадений с образцом

```js
const pos = str.indexOf('name');     // с начала (-1 не найдено)
const pos = str.indexOf('name', 2);  // поиск начат с позиции 2
const pos = str.lastIndexOf('name'); // с конца (-1 не найдено)
```
