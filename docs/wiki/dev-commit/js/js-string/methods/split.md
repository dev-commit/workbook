# split()

arr.join() - обратное преобразование

## Базовое использование

```js
// Преобразование строки с заданным разделителем в массив
const arr = str.split(';');
```

## ReqExp

```js
const pattern = /[\s,]+/;
const result = text.split(pattern); // разделитель пробел
```
