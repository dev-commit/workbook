# charAt(), charCodeAt()

```js
// возвращает символ строки по индексу
const root = charAt(2);
// возвращает код символа строки по индексу (все русские буквы находятся в диапазоне от 1040 до 1103)
const root = charCodeAt(2);
```

```js
alert( "".charAt(0) ); // пустая строка
alert( "" [0] );       // undefined
```
