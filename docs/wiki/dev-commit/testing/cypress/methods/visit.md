# Общие

```js
cy.visit('http://localhost:8081/index');  // открыть url для теста
cy.pause();                               // остановить выполнение и продолжить вручную
cy.wait(1000);                            // задержка перед выполнением следующего действия
```
