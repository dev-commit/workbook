# Стилизация активной ссылки

- при совпадении маршрута &lt;router-link&gt; автоматически получает класс &lt;router-link&gt; представляет собой ссылку &lt;a&gt;

```css
.router-link-active {
	/* */
}
```
