# Пакеты

```bash
sudo apt-get install git
sudo apt-get install p7zip-rar rar unrar unace arj cabextract # архиваторы
```

```bash
sudo apt install ubuntu-restricted-extras # шрифты windows
sudo apt install gnome-tweak-tool         # расширение рабочего стола
sudo apt install chrome-gnome-shell       # расширения для браузеров
```
