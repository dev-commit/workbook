# Пакеты

## Команды

```bash
sudo apt-get install НазваниеПакета
sudo apt install НазваниеПакета
```

## Пакеты

### Установка через `apt-get`

- `git`
- `p7zip-rar rar unrar unace arj cabextract` - архиваторы

### Установка через `apt`

- `ubuntu-restricted-extras` - шрифты windows
- `gnome-tweak-tool` - расширение рабочего стола
- `chrome-gnome-shell` - расширения для браузеров
