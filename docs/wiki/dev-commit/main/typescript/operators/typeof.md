# typeof

- TypeScript добавляет оператор typeof, который можно использовать в контексте типа для ссылки на тип переменной или свойства

```js
const s = 'Hello';

const n: typeof s;
// const n: string;
```
