# Символ !

- Символ ! говорит TS, что ты уверен, что там точно есть значение
- Если number | undefined, то при подствновке ! значение будет number
- Так лучше не делать

```js
const { data } = application.data!;
```
