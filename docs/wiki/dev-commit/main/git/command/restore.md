Откатить изменения

```bash
# Откатить изменения в файле на момент последнего коммита
git restoreFileName# Аналогично с checkout
git checkoutFileName# Откатить изменения в файле после добавления файла в Индекс
git restore --stagedFileName```
