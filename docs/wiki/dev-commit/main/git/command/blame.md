Инфорация об авторах коммита конкретного файла

```bash
git blameFileName# показать список авторов файла
git blame --date-short # укоротить дату
git blame -L 5,8       # указать интересующие строки
```
