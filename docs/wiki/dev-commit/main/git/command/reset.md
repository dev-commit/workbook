Отмена изменений. Изменяет указатель HEAD и, опционально, состояние индекса.
    
- Передвигает ветку на указанный коммит
- Сбросить коммиты до начального в ветке
- Убрать из индексации все файлы и директории, которые возможно уже были проиндексировалы с помощью git add

```bash
git reset HEAD~          # отменить 1 коммит
git reset HEAD~1         # отменить 1 коммит
git reset HEAD^          # отменить 1 коммит
git reset HEAD^1         # отменить 1 коммит
git reset HEAD^2         # отменить 2 коммита
git resetHashCommit# откатиться к конкретному коммиту (хэш из "git log")
git reset HEADFileName# если изменения были индексированны и нужно откатиться перед самим коммитом
```

## Виды reset

- Каждый вид reset позволяет откатиться к указанному коммиту

```bash
git reset--softHEAD~   # мягкий:ОСТАВИТЬпоследующие коммиты
git reset--hardHEAD~   # жёсткий:УДАЛИТЬпоследующие коммиты
git reset--mixedHEAD~  # смешанный: среднее между --soft и --hard (по умолчанию)
git reset--keepHEAD~   # жёсткий с сохранением изменений
git reset--mergeHEAD~  # жёсткий для отмены неудачного merge
```
- --soft - только перемещает ветку на указанный коммит. "working directory" и "index" без изменений
- --mixed - перемещает ветку на указанный коммит, сбрасывает "index", но не трогакт "working directory"
- --hard - сбрасывает "working directory" и "index" (состояние проекта переключается на указанный коммит)

<!-- <img src="../@img/reset-table.jpg" width="600px"/> -->
