# $effect

## Информация

::: tip `$effect`

- **$effect** - запускается при монтировании компонента и при каждом изменении зависимостей
  :::

## Структура

- Нельзя указать конкретную зависимость на update

```tsx
$effect(() => {
  //
});
```
